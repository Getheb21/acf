{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "apt-get update && apt-get install -y chromium-browser xvfb wget && pip install -r requirements.txt"
    },
    "deploy": {
        "startCommand": "xvfb-run --auto-servernum python main.py",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
